Aggregate Function :
....................

Aggregate function perform a calculation on a set of values, and return a single value.

▸ COUNT() :
  .........

▸ MAX() : To selec maximum in column
  .......
        Example :
            SELECT MAX(marks) FROM Student;

▸ MIN() :
  .......

▸ SUM() :
  .......

▸ AVG() :
  .......