Here is the complete, organized list of SQL (MySQL) functions — including aggregate functions, numeric, string, date, window, statistical, and other useful functions like VARIANCE.

This is the best cheat sheet for revision in interviews and exams.

⸻

📌 1. SQL Aggregate Functions (MOST IMPORTANT)

These work on groups of rows:

Function	Meaning
COUNT()	Counts number of rows
SUM()	Adds values
AVG()	Calculates average
MIN()	Minimum value
MAX()	Maximum value
VARIANCE()	Variance (spread) of values
STD() / STDDEV()	Standard deviation
GROUP_CONCAT()	Combines values into one string


⸻

📌 2. SQL Numeric Functions

Used for calculations:

Function	Meaning
ABS(x)	Absolute value
CEIL(x)	Round upward
FLOOR(x)	Round downward
ROUND(x, d)	Round to d decimal places
POWER(x, y)	x^y
SQRT(x)	Square root
MOD(x, y)	Remainder
RAND()	Random number
SIGN(x)	Returns sign (−1, 0, 1)


⸻

📌 3. SQL String Functions

Used for text and character operations:

Function	Meaning
LENGTH(str)	Length of string
LOWER(str)	Convert to lowercase
UPPER(str)	Convert to uppercase
TRIM(str)	Remove leading/trailing spaces
LTRIM(str)	Remove left spaces
RTRIM(str)	Remove right spaces
SUBSTRING(str, start, len)	Extract substring
LEFT(str, n)	Left n characters
RIGHT(str, n)	Right n characters
CONCAT(a, b, …)	Join strings
REPLACE(str, old, new)	Replace text
INSTR(str, substr)	Position of substring
REVERSE(str)	Reverse string
LPAD(str, n, pad)	Left padding
RPAD(str, n, pad)	Right padding


⸻

📌 4. SQL Date & Time Functions

Used to handle dates:

Function	Meaning
NOW()	Current date & time
CURDATE()	Current date
CURTIME()	Current time
DATE()	Extract date part
MONTH()	Extract month
YEAR()	Extract year
DAY() / DAYOFMONTH()	Extract day
HOUR()	Hour value
DATE_ADD(date, INTERVAL n unit)	Add time
DATE_SUB(date, INTERVAL n unit)	Subtract time
DATEDIFF(d1, d2)	Difference in days
TIMESTAMPDIFF(unit, d1, d2)	Time difference
LAST_DAY(date)	Last day of month


⸻

📌 5. Conditional Functions

Function	Meaning
IF(condition, true, false)	If-else
IFNULL(x, value)	Replace NULL
NULLIF(a, b)	Returns NULL if equal
CASE WHEN THEN	Conditional logic


⸻

📌 6. Statistical Functions

Includes VARIANCE:

Function	Meaning
VARIANCE(x)	Spread of values
STDDEV(x)	Standard deviation
STDDEV_POP(x)	Population SD
VAR_POP(x)	Population variance


⸻

📌 7. Window Functions (Advanced SQL)

Used with OVER():

Function	Meaning
ROW_NUMBER()	Row numbering
RANK()	Ranking (ties allowed)
DENSE_RANK()	Ranking (no gaps)
NTILE(n)	Divide into n buckets
LEAD()	Next row value
LAG()	Previous row value
FIRST_VALUE()	First value
LAST_VALUE()	Last value


⸻

📌 8. Mathematical & Trigonometric Functions

Function	Meaning
SIN(x)	Sine
COS(x)	Cosine
TAN(x)	Tangent
PI()	Returns π


⸻

📌 9. JSON Functions (Optional)

Function	Meaning
JSON_EXTRACT()	Get value from JSON
JSON_SET()	Modify JSON
JSON_ARRAY()	Create JSON array


⸻

⭐ If you want, I can create a PDF cheat sheet for all SQL functions for your exam.