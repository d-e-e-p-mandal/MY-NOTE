Creating Database :
...................

CREATE DATABASE db_name;

*if database is alreday exist of same name it gives error so we will use if not exists - it generate warning

CREATE DATABASE IF NOT EXISTS db_name;