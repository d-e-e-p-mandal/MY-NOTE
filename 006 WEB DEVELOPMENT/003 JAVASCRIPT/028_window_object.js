/* Window : 
    The window object represent an window in a browser. It is browser's object(not javascript)
    and automactically created by browser.

    It is a glowbal object with lots of properties and methods.
*/
/* DOM : when a web page loaded, the browser creats a document object model.
DOM tree :
    1.text-node
    2.comment node
    3.elements node

    DOM Manupulation : 
    tagName : return tag name for elements node
    innerText : return the text content of the element and all its children
    innerHTML : return the plan text content or HTML content in the element
    textContent : return textual content even for hidden elements
*/
console.log(window); 
window.console.log("hlw world!"); //by default window
